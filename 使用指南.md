# SolvForge 使用指南

## GitHub 发布包说明

这是可直接分发的 Windows x64 便携版。适用于 Windows 10/11 64 位电脑；不需要安装 Python、.NET、GROMACS 或 Packmol。请从 GitHub 下载 ZIP 后“全部解压”到任意有读写权限的目录，再双击 `SolvForge-GUI.cmd`。启动脚本始终以自身所在目录为基准，不依赖作者电脑的盘符、用户名或固定安装路径；整个文件夹可以移动到其他位置。

如果 Windows 弹出安全提示，请先在文件属性中选择“解除锁定”，再重新解压并运行。不要直接在 ZIP 压缩包内运行，也不要单独复制 `solvforge.exe`，否则内置模板和示例路径可能不完整。

## 图形界面（推荐）

不熟悉命令行时，直接双击目录中的 `SolvForge-GUI.cmd`。界面支持浏览选择材料、添加一个或多个 PDB 分子模板、直接填写数量或比例、按密度完整填充、周期方向、选择 GRO/CIF 输出格式、运行日志和结果打开。生成完成后窗口会保持打开，可以继续修改参数并再次生成。多组分情况下的临时配比文件由界面后台自动生成，不需要用户准备 TSV。

速度档位说明：`快速（推荐）` 使用自适应放置和 1.5 Å 候选网格；`平衡` 使用 1.0 Å 网格；`精细` 保留 complete 填充。速度档位只改变搜索策略，不改变输出格式。底层核心也已启用空间分桶、有限候选采样和有上限的精修，较大体系会明显快于原版。

填充判据也已调整：不再用整个溶剂分子的最大包围球过度排除碳层附近空间，而是使用适度的中心点预筛选，最后仍由原子级安全距离做严格重叠检查。这样薄碳层两侧的可用空间可以正常填充。

`complete` 模式现在会真正覆盖整个晶胞内可容纳分子的自由空间；在 `surface + complete` 下不再受默认 10 Å 表面壳层限制。使用密度时，目标分子数也按整个可用自由体积估算。空间小于完整分子尺寸、或违反原子安全距离的区域仍不会填入。

SolvForge 是一个不依赖 Python 的 Windows x64 原生程序，用于在材料的三维自由体积中生成溶剂或电解液构型。它支持 MOF、COF、沸石等孔道，也支持 slab、沟壑、台阶、粗糙表面和真空层。

程序通过材料 CIF 建立自由体积场，再对 PDB 分子进行连续随机平移、随机旋转、重叠检查和迭代优化，默认输出标准 GROMACS `.gro` 文件，同时生成 CIF 检查副本。网格只用于快速定位自由空间，不会把最终分子中心锁在规则网格上。GRO 输出会保留 PDB 中的显式虚原子；内置水模板 `water_tip4pew`、`water_tip5p` 和 `water_opc` 已包含 `MW`/`LP1`/`LP2`。如果 GRO 模式检测到水模板只有 O/H 实体原子，程序会自动换用带显式虚原子的对应模板（普通三点水默认升级为 TIP4P-Ew 几何），并在报告中记录替换。它是独立的原生程序，不要求安装 Python 或其他外部建模软件。

## 1. 安装

1. 解压程序包。
2. 在解压目录双击 `install_path.cmd`，或在 PowerShell 中运行：

```powershell
./install_path.cmd
```

如果系统只允许手动调用 PowerShell，也可以运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ./install_path.ps1
```

3. 重新打开 PowerShell，即可在任意目录调用：

```powershell
solvforge.exe --help
```

也可以不加入 PATH，直接在程序目录中运行 `./solvforge.exe`。

## 2. 最简单的单组分填充

材料默认使用 CIF，溶剂默认使用程序包中的 PDB 模板：

```powershell
solvforge.exe --material material.cif --solvent water_tip3p --number 200 --output filled.gro
```

也可以按密度估算分子数：

```powershell
solvforge.exe --material material.cif --solvent water_tip3p --density 1.0 --output filled.gro
```

一般建议首次建模使用 `--number`，这样分子数量完全可控；需要模拟明确密度时再使用 `--density`。

## 3. 内置正式案例

程序包只保留两个正式电解液案例，另附一个用于测试异型表面的沟壑 slab 几何文件。

### 案例 A：沟壑 slab + 约 1 M 硫酸锌水溶液

```powershell
solvforge.exe --material examples/rugged_slab_demo.cif --solution examples/zinc_sulfate_aqueous_1M.tsv --number 60 --mode surface --fill adaptive --output rugged_slab_ZnSO4_1M.cif
```

这个小型案例包含约 58 个水分子、1 个 Zn2+ 和 1 个 SO4^2-，用于验证沟壑、台阶和异型表面的放置能力。

### 案例 B：IRMOF-10 孔道 + 约 1 M LiPF6 电解液

```powershell
solvforge.exe --material raspa2/mofs/cif/IRMOF-10 --solution examples/lipf6_ec_dmc_1M.tsv --number 112 --fill adaptive --output IRMOF-10_LiPF6_1M.cif
```

这个案例的配比为 54 个 EC、54 个 DMC、2 个 Li+ 和 2 个 PF6-，用于验证 MOF 孔道中的多组分电解液填充。

## 4. 多组分溶液文件

`--solution` 文件是普通 TSV 或空格分隔文本，每行写：

```text
模板名    分子分数
```

例如：

```text
ethylene_carbonate  0.48214286
dimethyl_carbonate  0.48214286
Li                  0.01785714
PF6                 0.01785714
```

分数不要求提前严格加和为 1，程序会自动归一化。程序根据 `--number` 和分子分数计算各组分的整数数量。单原子离子会自动启用更严格的自由空间保护，避免被塞进狭窄孔口或奇怪的小空间。

查看内置溶剂和离子模板：

```powershell
solvforge.exe --list-solvents
```

查看内置材料：

```powershell
solvforge.exe --list-materials
```

程序包还提供一个常用碱性电解液配比文件：

```text
solutions/KOH_KNO3_1M_0p2M.tsv
```

它对应约 1 M KOH + 0.2 M KNO3：K+ 总浓度约 1.2 M，OH- 约 1.0 M，NO3- 约 0.2 M。

## 5. PDB 分子模板在哪里、如何指定

程序包内置的溶剂和离子 PDB 模板位于：

```text
templates/solvents/
```

查看所有内置模板：

```powershell
solvforge.exe --list-solvents
```

单组分填充时，可以直接写内置模板名：

```powershell
solvforge.exe --material material.cif --solvent ethylene_carbonate --number 100 --output filled.cif
```

也可以直接指定自己的 PDB 文件路径：

```powershell
solvforge.exe --material material.cif --solvent D:/my_templates/EC.pdb --number 100 --output filled.cif
```

多组分溶液中，TSV 第一列既可以写内置模板名，也可以写 PDB 路径：

```text
D:/my_templates/EC.pdb       0.48214286
D:/my_templates/DMC.pdb      0.48214286
D:/my_templates/Li.pdb       0.01785714
D:/my_templates/PF6.pdb      0.01785714
```

然后运行：

```powershell
solvforge.exe --material IRMOF-10.cif --solution solution.tsv --number 112 --output electrolyte.cif
```

自定义 PDB 的要求：

- 每个 PDB 文件应只包含一个完整分子或一个完整离子模板；
- 使用 `ATOM` 或 `HETATM` 记录；
- 坐标单位为 Å；
- 原子名称或元素列应正确；
- 程序会把整个 PDB 当作一个刚性分子进行旋转和平移；
- PDB 中的电荷和力场参数不会自动读取，正式 MD 前仍需另外赋值；
- GRO 输出中的虚原子必须在 PDB 中有坐标；只有“由力场生成”的注释不会自动产生虚原子；
- `water_tip4pew`、`water_tip5p` 和 `water_opc` 内置模板会直接输出显式虚原子；无虚原子的水模板在 GRO 模式下自动替换；
- 路径中尽量不要包含空格。

## 6. 材料输入

- 推荐：CIF；
- 兼容：POSCAR/VASP；
- 兼容：带 `--cell A B C` 参数的 XYZ；
- 支持正交和一般非正交晶胞；
- 程序包内置 RASPA2 structures 中的材料 CIF，可用相对路径调用；
- 输入材料中的晶胞尺寸和周期边界必须合理，否则自由体积判断也会失真。

例如：

```powershell
solvforge.exe --material my_mof.cif --solution examples/lipf6_ec_dmc_1M.tsv --number 112 --output my_mof_electrolyte.cif
```

## 7. 模式选择

通常不需要写 `--mode`，程序会自动判断。必要时可以手动指定：

| 模式 | 用途 |
|---|---|
| `surface` | slab、沟壑、台阶、粗糙表面和真空层 |
| `pore` | MOF、COF、沸石等周期性孔道 |
| `accessible` | 填充所有满足几何间隙要求的空间 |
| `ion` | 只放置离子，并启用离子保护策略 |

异型表面示例：

```powershell
solvforge.exe --material slab.cif --solution examples/zinc_sulfate_aqueous_1M.tsv --number 200 --mode surface --side above --output slab_ZnSO4.cif
```

孔道示例：

```powershell
solvforge.exe --material zeolite.cif --solvent water_tip3p --number 500 --mode pore --periodic xyz --output zeolite_water.cif
```

## 8. 常用参数

| 参数 | 含义 | 默认值 |
|---|---|---|
| `--material` | 材料 CIF、POSCAR 或材料库名称 | 必填 |
| `--solvent` | 单一 PDB 模板名或 PDB 路径 | water_tip3p |
| `--solution` | 多组分溶液 TSV 文件 | 无 |
| `--number` | 总分子数 | 与 density 二选一 |
| `--density` | 溶液质量密度，g/cm3 | 1.0 |
| `--mode` | surface/pore/accessible/ion | 自动 |
| `--fill` | standard/adaptive/complete | complete |
| `--periodic` | 周期方向，例如 xy 或 xyz | surface 为 xy，pore 为 xyz |
| `--side` | both/above/below，控制 slab 哪一侧 | both |
| `--tolerance` | 分子间及材料间安全距离，Å | 1.5 |
| `--material-solvent-min-distance` | 材料原子中心到溶剂原子中心的最近距离，Å；按实际原子坐标检查 | 3.0 |
| `--surface-fill-range` | 从材料原子中心纳入填充的范围，Å；0 表示整个自由空间 | 0 |
| `--grid` | 自由体积网格间距，Å | 1.0 |
| `--ion-min-free-space` | 离子所需的最低自由空间，Å | 4.0 |
| `--solution-z-min/max` | 限制电解液的 Z 范围，Å | 不限制 |
| `--seed` | 随机种子，便于重复计算 | 20260826 |
| `--output` | 输出文件名；推荐使用 `.gro`，同时生成同名 `.cif` 检查副本 | 必填 |
| `--wait` | 命令行生成完成后等待 Enter，再关闭窗口 | 直接打开程序时自动启用 |
| `--no-wait` | 后台调用时生成完成后直接返回 | GUI 自动使用 |

多数情况下只需要 `--material`、`--solution`、`--density` 和 `--output` 四个参数。GUI 会自动使用 `complete`，生成结束后工作台保持打开，可直接修改参数并再次生成；直接双击命令行版时，输出完成后窗口会自动保持打开，按 Enter 才关闭。后台脚本或 GUI 子进程使用 `--no-wait`。

## 9. 输出文件

GUI 中可以自主选择 GRO 或 CIF。选择 GRO 时同时生成 CIF 检查副本；选择 CIF 时主输出为 CIF。界面底部显示开发者微信：`x1aohua501`。

例如输出文件为 `filled.gro`，程序会生成：

- `filled.gro`：标准 GROMACS 结构文件，坐标单位为 nm，可直接作为后续水模型/电解液模拟的结构输入；
- `filled.cif`：同一结构的 CIF 检查副本，可直接用 VESTA 打开；
- `filled.free_volume.cube`：三维自由体积场，可用 VMD 等软件检查；
- `filled.gro.report.json`：放置数量、组分统计、重叠拒绝次数和计算状态。

重点检查报告中的 `status`、`placed_molecules`、`rejected_material_overlap` 和 `rejected_solvent_overlap`。

如果 `status` 不是 `complete`，应降低 `--number`、增大晶胞、减小分子尺寸或检查材料 CIF 是否正确。

## 10. 推荐工作流程

1. 用 VESTA 检查材料 CIF 的晶胞、周期性和原子位置。
2. 先用较小的 `--number` 测试，例如 20 或 60。
3. 查看输出 GRO、CIF 检查副本和 `.report.json`。
4. 用 VMD 查看 `.free_volume.cube`，确认自由体积区域符合预期。
5. 再逐步增加分子数，或使用 `--density` 建立目标密度。
6. 在正式 MD 前进行能量最小化和短时间预平衡。

## 11. 重要说明

SolvForge 生成的是几何上安全的初始构型，不会自动完成力场赋值、离子电荷分配、键参数生成或分子动力学平衡。Li+、PF6-、Zn2+、SO4^2- 等模板可用于插入和结构检查；正式 MD 前必须根据所用力场补充电荷、质量、键、角度和非键参数。

GRO 文件保存坐标、残基/原子名称、晶胞以及 PDB 中已有的显式虚原子，但不包含水模型的电荷和完整力场参数。要切换 TIP3P、TIP4P/2005 等水模型，应使用对应的 PDB 原子命名或后续拓扑工具，并配套加载相应的 `.itp`/力场参数；SolvForge 不会把一种水模型的参数硬编码进另一种模型。

程序采用自由体积候选单元、单元内部连续随机平移、空间分桶碰撞检查和随机顺序插入。`adaptive` 适合常规目标数量；`complete` 会按整个可用自由体积达到密度目标，并继续尝试填补较大的残余空腔。大体系的 complete 全局模式会跳过昂贵的全对全精修，因此速度更快；始终保留安全间距，不会为了追求几何上的零间隙而强行塞入分子。

材料–溶剂最近距离定义为材料原子中心到溶剂原子中心的实际最近距离，不等同于分子中心距离，也不等同于通用碰撞容差。这个距离会在自由体积网格生成阶段先参与候选空间筛选，分子放置后再用实际原子坐标复核，所以修改它会改变可填充空间，而不只是改变最后一步的拒绝数量。表面填充范围为 0 时不限制外层范围；输入正值后，只把距离材料原子中心不超过该范围的区域纳入填充。
